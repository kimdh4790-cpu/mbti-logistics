# 배송앱 (emergency.html) 변경 내역 및 설계 문서

> 파일: `emergency.html`, `_worker.js`  
> 서비스 URL: `mbtico.kr/{슬러그}/{앱명칭}`  
> 최종 수정: 2026-09-17  
> **다른 세션에서 이 파일을 먼저 읽고 작업할 것**

---

## 🚫 절대 건드리면 안 되는 것

### 무한루프 방지 구조 — 절대 변경 금지
```
_coNameLoaded 플래그 제거 금지
_emerLoadCoName() .then() 콜백 안에서 _emerInitLogin() 호출 금지
```
- 이 두 가지를 건드리면 Firestore 무한 읽기 루프 재발 (706건+/페이지로드)
- 수정 전 반드시 이 파일 읽을 것

### Firestore onSnapshot 추가 금지
- 배송앱은 **전부 `.get()` 방식** (일회성 조회)
- `onSnapshot` 추가 시 탭 열어두는 동안 계속 읽기 발생 → 5만/일 한도 초과
- 실시간이 필요한 기능은 `setInterval + fetch API` 방식으로 구현

### URL에 `?did=` 추가 코드 재추가 금지
- `history.replaceState`로 `?did=` 붙이는 코드 이미 제거됨
- 슬러그 URL(`mbtico.kr/mbti`)이 표준 — `?did=` 노출은 UX 문제

### Worker KV 캐시 키 형식 고정
- `/api/emergency-map` 캐시 키: `emap:{dealerId|driver}:{date}` (TTL 60초)
- 형식 바꾸면 캐시 미스로 불필요한 Firestore 읽기 재발

### `_worker.js` 2단계 슬러그 핸들러 제거 금지
- `/{slug}/{appSlug}` 라우팅 블록이 없으면 앱별 URL 전체 불능
- 반드시 단일 슬러그 핸들러보다 **앞에** 위치해야 함

---

## 🗺 전체 구조 개요

### URL 체계
| URL | 동작 | 용도 |
|-----|------|------|
| `mbtico.kr/mbti` | `_pwaAppName` 없음, 앱 선택 가능 | 단기·멀티앱 기사 |
| `mbtico.kr/mbti/롯데` | `_pwaAppName='롯데'` 주입, 앱 고정 | 고정 담당 기사 |
| `mbtico.kr/emergency?did=UID` | 구형 폴백 (여전히 동작) | 레거시 접근 |

### 역할 분리
| 역할 | 로그인 방식 | localStorage 키 | 진입 화면 |
|------|-----------|----------------|---------|
| 기사 | 구역 선택 + 이름 | `mbti_emer_driver`, `mbti_emer_camp` | 배송 입력 화면 |
| 관리자 | 이메일+비밀번호 Firebase Auth | `mbti_emer_role='admin'` | 관제 패널 |

### 앱 고정 vs 선택
| 구분 | `_pwaAppName` | 서비스 버튼 | 정산 `appName` |
|------|-------------|-----------|--------------|
| 고정 앱 URL | Worker가 주입 | 비활성(고정 표시) | 주입된 앱명 |
| 단기 앱 URL | 없음 | 선택 가능 | 선택한 앱명 |
| 구형 `?did=` | 없음 | 선택 가능 | 선택한 앱명 |

### Firestore 데이터 구조
```
companies/{dealerId}
  ├── companyName: "엠비티아이"
  ├── emergencyPrices: { "강서구": 5000, "해운대구": 6000, ... }
  ├── emergencyServices: ["쿠팡", "CJ대한통운", "롯데택배"]
  ├── emergencyDrivers: { "홍길동": ["쿠팡", "롯데택배"], ... }
  └── slug: "mbti"

emergency_deliveries/{docId}
  ├── dealerId, driver, camp, date, appName  ← appName 필수
  ├── trackings: [...], addr, memo, photos
  └── status: "대기" | "완료"
```

### Worker API 엔드포인트
| 엔드포인트 | 메서드 | 설명 | KV 캐시 |
|-----------|--------|------|---------|
| `/api/emergency-save` | POST | 배송 저장 | 없음 |
| `/api/emergency-update` | POST | 배송 수정 (운송장 병합, 완료 처리) | 없음 |
| `/api/emergency-map` | GET | 오늘 배송 목록 | **60초** |
| `/api/emergency-list` | GET | 배송 기록 (날짜 범위) | 없음 |
| `/api/emergency-register` | POST | 기사 앱 자동 등록 | 없음 |
| `/api/emergency-drivers` | GET | 기사 목록 + 앱 배정 현황 | 없음 |
| `/api/emergency-driver-apps` | POST | 관리자 기사 앱 배정 저장 | 없음 |
| `/api/emergency-prices` | GET/POST | 단가+서비스 조회/저장 | 없음 |
| `/api/emergency-delete` | POST | 배송 삭제 | 없음 |
| `/api/emergency-photo` | POST | 사진 추가 | 없음 |
| `/api/emergency-stats` | GET | 오늘 통계 카운터 | 없음 |
| `/api/emergency-apikeys` | GET | 카카오/Google API 키 | 없음 |

---

## 앱별 동작 차이

### 고정 앱 (`mbtico.kr/mbti/롯데`)
- Worker가 HTML `<head>`에 `window._pwaAppName='롯데'` 스크립트 주입
- 기사 로그인 화면 서비스 선택 버튼 → 비활성화, "롯데 전용" 고정 표시
- 로그인 시 `/api/emergency-register` 자동 호출 → Firestore에 기사 등록
- PWA 설치 시 `start_url: '/mbti/롯데'` → 재접속해도 앱 고정 유지
- manifest: `mbtico.kr/mbti/롯데/manifest.json`

### 단기 앱 (`mbtico.kr/mbti`)
- `_pwaAppName` 없음
- 로그인 화면에서 서비스(앱) 버튼 선택 가능
- 선택한 앱명이 `localStorage('mbti_emer_app')`에 저장
- 배송 저장 시 해당 앱명으로 `appName` 필드 기록

### 관리자 패널
- 기사 현황 탭: **2분** 자동 갱신 (변경 금지 — Firestore 읽기 절약)
- 단가 설정 탭: 슬러그 입력 → 저장 시 KV에 `company-slug:{slug}` 등록
- 정산 탭: 기사 → 앱명칭 → 구역 순 그룹화, 각 구역 단가 적용

---

## Firestore 읽기 현황

| 이벤트 | 읽기 횟수 | 비고 |
|--------|----------|------|
| 앱 최초 로드 | 1회 | `companies/{did}` |
| 기사 로그인 | 0~1회 | localStorage에 did 없을 때만 `users/{uid}` |
| 배송 저장 (운송장 4자리 이상) | 1회 | 중복 핀 체크 |
| 배송 저장 (운송장 없음) | 0회 | 직접 저장 |
| 관리자 기사현황 | Worker 경유 | 2분마다 `/api/emergency-map` (KV 캐시 60초) |
| 정산/지도 | Worker 경유 | 탭 진입 시 1회 (KV 캐시 60초) |

**onSnapshot 없음** — 탭 열어두어도 연속 읽기 없음  
**100명 기사 기준 하루 예상 읽기**: ~2,000건 (5만 한도의 4%)

---

## Firebase Storage

- 배송 완료 처리 시 사진 업로드: `emergency_complete/{driver}/{timestamp}.jpg`
- 배송 등록 시 사진 업로드: `emergency_deliveries/{driver}/{timestamp}_0.jpg`
- Storage는 Firestore 읽기 쿼터와 **완전히 별개** (1GB/일 다운로드 무료)
- 9월 실운영 시 Storage 사용량 모니터링 필요

---

## 알려진 제한사항 및 향후 과제

- 관리자가 `?did=` URL로 직접 접근 시에도 동작 (슬러그 우선, 폴백 지원)
- PWA 홈화면 설치는 앱별 URL에서 설치해야 올바른 `start_url` 적용
- `/api/emergency-map` KV 캐시 60초 → 기사가 배송 저장 직후 지도 열면 최대 60초 반영 지연
- 9월 중순 실운영 예정 → Firebase Blaze 플랜 전환 필수 (현재 Spark 무료 플랜)

---

## 변경 이력

### 2026-09-18 (/contract/sign/ 서버 오류 긴급 수정)
- **`_worker.js`** sign 핸들러 `_cDetailTable` 내 `_dlCname`/`_dlCbiz` → `_sc_cname`/`_sc_cbiz` 교체
  - `_dlCname`, `_dlCbiz`는 download 핸들러 전용 변수로 sign 핸들러에서 undefined → ReferenceError → "서버 오류" 반환
  - FCM 오류 알림 "worker:1건" 원인. 3곳 모두 교체 완료

### 2026-09-17 (DONWAY 팝빌 세금계산서 연동 완성 — 키만 넣으면 작동)
- **`_worker.js`**:
  - `/api/stmt-tax-issue`: `receiverCorpNum` 하드코딩 폴백(`373-86-02536`) 제거 → `gs2('bizNum')||''`
  - `/api/popbill-status` 신규 엔드포인트: 어드민 인증 후 `{configured, testMode}` 반환 (키 설정 여부 확인용)
- **`donway-pages/index.html` `openTaxInvoice()`**:
  - 대리점(공급받는자) 정보 동적화: `window._ctrCompany||window._COMPANY_CFG||{}` 에서 `bizNum`, `companyName`, `ceoName`, `bizAddr` 읽음 (기존 `373-86-02536`/`엠비티아이유한회사`/`관리자`/`부산시 수영로 668` 하드코딩 제거)
  - 🧾 팝빌 발행 버튼 추가 (세금계산서 미리보기 모달 하단)
  - `_dwPopbillIssue()` 함수 신규: Firebase 토큰 발급 → `/api/popbill-issue` 호출 → 결과 toast
- **Cloudflare Secrets 설정으로 즉시 활성화**:
  - `POPBILL_LINK_ID`: 팝빌 LinkID
  - `POPBILL_SECRET_KEY`: 팝빌 SecretKey (Base64)
  - `POPBILL_TEST_MODE=false`: 운영 모드 (미설정 시 테스트 모드)
  - 미설정 시 에러 메시지 정상 노출, 설정 즉시 운영 발행 가능

### 2026-09-17 (계약서 iframe 블랭크 수정 — 파일 프록시 경유)
- **원인**: Google Docs Viewer가 Firebase Storage URL에 직접 접근 불가 (인증 토큰 필요) → iframe 공백
- **수정** (`_worker.js`): 세 경로 모두 Google Docs Viewer URL을 `customContractUrl` 직접 인코딩 → `/contract/file-proxy/{token}` 프록시 URL 인코딩으로 변경
  - 사전 서명 뷰 (`_cProxyUrl`, `_cToken`)
  - 아카이브 본문 (`_aProxyUrl`, `signToken`)
  - 다운로드 핸들러 (`_dlProxyUrl`, `_dlToken`)
- 프록시 엔드포인트(`/contract/file-proxy/{token}`)가 Worker에서 Firebase Storage → 공개 서빙하므로 Google Docs Viewer 정상 로드됨

### 2026-09-17 (DONWAY 계약서 업로드 파일 연동 — 드라이버 서명 화면)
- **아카이브 본문 교체** (`_worker.js`): `customContractUrl` 있을 때 표준 조항 텍스트 대신 업로드 파일 iframe + 서명 블록으로 대체 (기존 배너 prepend 방식 제거)
  - PDF: 직접 iframe 임베드 / DOCX 등: Google Docs Viewer 경유
  - 서명 블록(위탁자(갑)/수탁자(을) 정보+도장/서명 이미지) 직접 재구성
- **다운로드 핸들러 교체** (`_worker.js`): `customContractUrl` 있을 때 동일한 iframe + 서명 블록으로 `_dlBody` 대체 (`_dlCustomUrl` 분기 추가)
- **사전 서명 뷰 교체** (`_worker.js`): 이전 세션에서 완료 — `_contractViewHtml`이 iframe 표시 (이미 적용)
- 세 경로 모두 통일: 대리점이 업로드한 원본 계약서 파일을 iframe으로 표시, 표준 조항 텍스트는 파일 없을 때만 폴백

### 2026-09-17 (서명 완료 후 기사가 동일 계약서 즉시 확인)
- **목적**: 기사가 서명 저장 후 대리점이 보는 것과 100% 동일한 계약서 파일을 즉시 확인
- **`_archiveResultUrl` 변수** (`_worker.js`): Firebase Storage 업로드 성공 시 `_archiveResultUrl = _aUrl` 할당
- **POST 응답 수정** (`_worker.js`): `{ok:true}` → `{ok:true, archiveUrl: _archiveResultUrl}` 반환
- **클라이언트 리다이렉트** (`_worker.js`): `location.reload()` → `location.href = data.archiveUrl` (서명 완료된 전체 HTML 파일로 즉시 이동)
- 폴백: archiveUrl 없을 경우 `/contract/sign/{token}` 유지

### 2026-09-17 (DONWAY 계약서 DOCX 빈칸 자동 채우기)
- **mammoth 렌더 후 클라이언트 fill** (`_worker.js`): 서명 전 미리보기에서 DOCX를 mammoth로 변환한 HTML에 빈칸 자동 채우기 추가
- 수탁자 이름 (`택배종사자인___` 패턴), 계약기간 (`년 월 일부터 년 월 일까지` 패턴), 집화수수료 (`1건당 0 원`), 배송수수료 (`1건당 ___원`), 차량번호 자동 채움
- Firestore 데이터를 `JSON.stringify`로 직렬화 → 클라이언트 `var _d` 객체에 주입 → 정규식으로 패턴 교체
- 채워진 텍스트는 파란색 bold (`color:#0066ff`) 강조
- 커밋: `10193dd0` → auto-merge → main 반영

### 2026-09-17 (계약서 서명 후 기사 정보 누락 수정)
- **서명 페이지 입력 필드 추가** (`_worker.js`): 기사 서명 카드에 사업자등록번호(pre-fill)·주민등록번호 입력 필드 추가
- **`submitSign()` 수정** (`_worker.js`): 입력 값을 읽어 `driverBizNum`, `driverIdNum` POST body에 포함
- **백엔드 destructure 확장** (`_worker.js`): `/api/contract/sign-driver` 핸들러에 두 필드 기본값 추가
- **Firestore PATCH 확장** (`_worker.js`): updateMask + fields에 `driverBizNum`, `driverIdNum` 추가
- **아카이브 수정** (`_worker.js`): `_aBiz` = 요청 body 우선·Firestore 폴백, `_aIdNum` 추가, `_aSignDate` = 서명 시각 기준 날짜 (기존 `_aDateStr`=생성일 대신), 동의 섹션 양쪽에 서명일·사업자번호·주민등록번호 표시
- **다운로드 핸들러 수정** (`_worker.js`): `_dlIdNum` 읽기 추가, 동의 섹션에 주민등록번호 표시
- 커밋: `e5d5da07` → auto-merge → main 반영

### 2026-09-16 (계약서 노선별 단가 등록 — 라우트 단가표 연동)
- **노선별 배송수수료 등록 UI** (`donway-pages/index.html`):
  - `_ctrRenderConditions()`: 단일 담당구역+배송수수료 입력 → RPRICE 라우트 스크롤 목록으로 교체
  - 라우트별 체크박스 + 단가 RPRICE 프리필 + 직접 수정 가능 (max-height:200px 스크롤)
  - `_ctrRouteCheck()`: 체크 시 단가 input 활성화 헬퍼 신규 추가
  - `_ctrCollect()`: `routePricesJson` 배열 수집 + `route`/`unitPrice` backward compat 유지
  - `_ctrGenHTML()`: `routePricesJson` 있으면 노선별 배송수수료 다중 행 렌더링
  - `_ctrLoadStamp()`: stamp preview 60px → 50px 통일
- **Worker 서명 페이지 연동** (`_worker.js`):
  - `_sc_rps`: `routePricesJson` 파싱 변수 추가
  - 수수료 테이블: `routePricesJson` 있으면 노선별 다중 행, 없으면 기존 단일 행 fallback
  - fallback 계약서(`else` 브랜치)도 다중 노선 단가 인라인 표시

### 2026-09-16 (DONWAY 계약서 시스템 버그 수정)
- **Firebase Storage SDK 추가** (`donway-pages/index.html`): `firebase-storage-compat.js` 스크립트 태그 누락으로 "firebase.storage is not a function" 오류 발생 → 추가 완료
- **이중 도장 버그 수정** (`donway-pages/index.html`): `_CTR_STAMP_B64` 기본 도장가 항상 표시돼 업로드 도장와 중복 표시되던 버그 → `adminSig` 유무로 조건 분기
- **도장 크기 조정** (`donway-pages/index.html`, `_worker.js`):
  - 관리자 미리보기 서명칸: 120px → 72px (짤림 방지)
  - Worker 서명완료 페이지 `.sign-box img`: 160px → 80px
  - Worker 다운로드 페이지 `.sig-box img`: 200px → 90px
  - 업로드 미리보기: 80px → 60px
- **위탁자 회사명 오류 수정** (`donway-pages/index.html`, `_worker.js`):
  - `_ctrGenHTML()` 내 `data.companyName` → `var companyName='엠비티아이(유)'` 하드코딩 (위수탁/부속합의/퀵플렉스/근로 4개 계약서 모두 적용)
  - Worker sign 페이지: 이미 `_sc_cname = '엠비티아이(유)'` 하드코딩 확인

### 2026-08-21 (백화점 배송앱 신규)
- **`department.html` 신규 생성**: 백화점 배송 전용 독립 앱 (emergency.html과 별도)
  - 기사: 백화점 층/구역 선택 + 이름 입력 → 익명 로그인
  - 배송 등록 모달: 수취인·주소·연락처·배송방식·배송유형 선택
  - 배송 완료 처리: Firebase Storage `dept_complete/{driver}/{timestamp}.jpg` 사진 업로드
  - 기사 메인화면: 오늘 배송 목록 + 정산 현황 (배송유형별 단가 적용)
  - 관리자: 이메일+비밀번호 Firebase Auth → 기사현황(7일 탭) + 정산(7일 탭)
  - 정산: 기사 → 배송유형 → 백화점층 순 그룹화, `emergencyPrices` 단가 재사용
- **`_worker.js` 3개 엔드포인트 추가** (L9473 삽입):
  - `POST /api/dept-save` — `dept_deliveries` 컬렉션 저장 (dealerId 유효성 확인)
  - `GET /api/dept-list` — dealerId + 선택적 driver/date 필터, 최대 200건
  - `POST /api/dept-update` — 상태 업데이트 (docDealerId 소유권 검증)
- Firestore 컬렉션: `dept_deliveries` (신규) — `dealerId`, `driver`, `date`, `customerName`, `addr`, `phone`, `floor`, `deliveryType`, `memo`, `status`, `savedAt`

### 2026-09-17
- **배달대행 정산 전면 재설계** (donway-pages/index.html `pageDeliverySettle` 교체):
  - 기존 복잡한 시뮬레이터 엔진 → 쿠팡이츠 엑셀 파싱 기반 AI정산 스타일로 재설계
  - 쿠팡이츠 Excel 파일 업로드 시 **일자별 정산내역** 시트 자동 인식
  - 기사 성함(D열)으로 그룹화, 건수(E열)/금액(F열) 취합, 날짜(B열) 누적 저장
  - 매일 첨부 시 날짜별 누적 취합 (Firestore `delivery_sessions/{did}_coupang_{ym}`)
  - 세금 자동 계산: 원천세 3.3% / 산재보험 0.88% / 고용보험 0.8%
  - AI정산 스타일 테이블: 이름/건수/세전금액/원천세/산재/고용/실지급/상세
  - 상세 팝업: 일자별 건수·금액 + 세금 내역
  - 엑셀 다운로드: 정산요약/일자별상세/세무사자료 3시트
  - `pageDeliveryInput` 관리자→`pageDeliverySettle` 리다이렉트로 단순화
  - Firestore 저장 컬렉션: `delivery_sessions`, 문서ID 형식: `{did}_coupang_{ym}`

### 2026-08-17
- **날짜별 정산 탭**: 기사 메인화면 정산 박스에 최근 7일 날짜 탭 추가 — 탭 누르면 해당 날짜 완료 건수·금액 표시 (오늘은 캐시 map API, 과거는 list API)

### 2026-08-16 (지도 핀 클러스터링 · PWA · 동명이인)
- **배송 핀 클러스터링 개선**: `_baseAddr()` 시/군/구 접두어 제거 → 같은 건물 주소 정규화 일치, 동 주소 15m GPS 겹침 허용 → 동일 아파트동 배송이 한 핀으로 합쳐짐
- **호수 표시**: 수취인 없을 때(`미확인`) 주소에서 호수 자동 추출 표시 (지도 카드 + 팝업 공통)
- **PWA 자동 로그인**: 홈화면 설치 후 재진입 시 기존 기사 정보 있으면 로그인 화면 건너뜀
- **PWA 슬러그 manifest**: `/{slug}/manifest.json` Worker 핸들러 추가 → 설치 시 올바른 `start_url` 적용
- **동명이인 처리**: 기사 등록 시 같은 이름 존재하면 A·B·C 접미사 제안 UI
- **XSS 처리기록 모달**: `openEmergencyAdmin` 행 `_esc()` 적용 (`r.date`, `r.time`, `r.driver`, `detail`)

### 2026-08-16 (보안 점검)
- **정산 완료건만 집계** (`status==='완료'` 필터, 이전엔 전체 건수 집계)
- **정산 완료 N건 / 전체 N건 표시** (헤더 + 기사 소계 + 총합계 행)
- **XSS 방지**: `_esc()` 헬퍼 추가 → 기사현황 카드, 정산 테이블, 처리기록 모달 행 모두 적용
- **API 키 노출 방지**: `/api/emergency-apikeys` dealerId 검증 추가, 클라이언트 `localStorage` 저장 제거 → `window.*` 변수로만 보관
- **docDealerId null bypass 수정**: `if(docDealerId &&...)` → `if(!docDealerId||...)` 패턴으로 수정 (update/photo/delete 3개 엔드포인트)
- **사진 크기 제한**: `/api/emergency-photo` 3MB 초과 시 413 반환
- **단가 수정 인증**: `/api/emergency-prices` POST에 Firebase ID 토큰 검증 추가
- **기사 프로필 저장 인증**: `/api/emergency-driver-profile` POST dealerId 유효성 확인

### 2026-09-17 — DONWAY 계약서 위탁자(대리점) 정보 입력 필드 완성
- **donway-pages/index.html**: 계약 등록 폼에 ② 위탁자(대리점) 정보 섹션 추가
  - 입력 필드: 대리점 상호, 사업자등록번호, 대표자명, 대리점 전화번호, 사업장 주소
  - Firestore companies 문서에서 자동 입력, 직접 수정 가능
  - 기존 ② 기사 정보 → ③, ③ 계약 조건 → ④ 번호 재부여
  - `_ctrCollect()`: companyName/companyBiz/companyCeo/companyAddr/companyPhone 모두 폼 입력값 우선
- **_worker.js**: 다운로드·아카이브·서명완료 뷰에서 위탁자 정보 동적 처리
  - `_dlCname`/`_dlCbiz` (다운로드), `_aCname`/`_aCbiz` (아카이브) 변수 추가
  - `_sc_cname`/`_sc_cbiz`/`_sc_ceo`/`_sc_caddr` (서명 전 뷰) 이미 동적 처리
  - 모든 위탁자(갑) 서명블록: 상호·사업자번호·대표자·주소를 Firestore 저장값으로 대체
  - 개인정보 동의서 "귀하" 문구, 서명 하단 레이블 동적 처리
  - `aName`/`aBiz` (서명완료 뷰): `_sd('companyName')` 우선 사용

### 2026-09-17 — DONWAY 커스텀 계약서 대리점 입력 내용 표시
- **_worker.js**: 커스텀 계약서(`customContractUrl`) 3개 뷰 모두에 📋 계약 주요 내용 테이블 추가
  - 서명 전 뷰 (`/contract/sign/{token}`): `_cDetailTable` — 수탁자·계약기간·담당구역·캠프·수수료·지급일·차량번호·특약사항 표시 후 DOCX 원문 렌더링
  - 서명 완료 아카이브 HTML (`/api/contract/sign-driver`): `_aDetailTable` — 동일 내용
  - 다운로드 뷰 (`/contract/download/{token}`): `_dlDetailTable` — 동일 내용
  - DOCX(mammoth.js 인브라우저 렌더링)·PDF(iframe) 두 케이스 모두 적용
  - Firestore 필드명: `contractStart`, `contractEnd`, `area`, `route`, `camp`, `routePrices`, `unitPrice`, `collectPrice`, `cycle`, `carNum`, `specialTerms`
  - 기존 blank 템플릿만 보이던 문제 해결 — 대리점이 작성한 모든 계약 조건 기사도 확인 가능

### 2026-08-16
- URL `?did=` 오염 수정 (`history.replaceState` 제거)
- 링크복사 칸 슬러그 URL만 표시
- 관리자 모니터 갱신 30초 → **2분**
- `/api/emergency-map` KV 캐시 60초 추가 (저장/완료처리 시 즉시 무효화)
- 배송 사진 Storage 업로드 dead path 수정
- 불필요 코드 61줄 제거 (`if(false)` 블록, 미사용 함수 4개, 디버그 toast)
- `/api/company-slug` KV 저장 시 `emergency: true` 추가 → 신규 고객사 등록 시 슬러그 URL에서 배송앱 자동 노출
- 기사 최초 접속 시 정보 등록 화면 추가 (이름·연락처·주민번호·차량·통장) → Firestore `driver_profiles` 저장
- 관리자 기사현황 카드에 "정보 보기" 버튼 → 등록 프로필 모달 조회
- Worker: `/api/emergency-driver-profile` GET/POST 추가

### 2026-08 (이전 세션)
- Firestore 무한루프 수정 (`_coNameLoaded` 플래그)
- 관리자/기사 탭 분리
- 관리자 패널 3탭 (기사현황, 단가설정, 정산)
- `mbtico.kr/{slug}/{앱명}` 2단계 URL 라우팅
- 기사 자동 등록 (`/api/emergency-register`)
- 관리자 기사 앱 배정 매트릭스
- 서브타이틀 형식: `회사명 / 앱명칭`
- 정산 앱명칭별 분리 (`appName` 필드)
