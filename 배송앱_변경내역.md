# 배송앱 (emergency.html) 변경 내역

> 파일: `emergency.html`, `_worker.js`  
> 서비스 URL: `mbtico.kr/{슬러그}/{앱명칭}`  
> 최종 수정: 2026-08-16

---

## 현재 구조 요약

- **기사 접속**: `mbtico.kr/mbti` (단기·멀티앱) 또는 `mbtico.kr/mbti/롯데` (고정 앱)
- **관리자 접속**: 기사 로그인 화면 → 관리자 탭 → 이메일+비밀번호
- **슬러그 등록**: 관리자 → 단가 설정 탭에서 슬러그 입력 후 저장 → KV 자동 등록
- **Firestore 읽기**: 페이지당 최대 2회 (onSnapshot 없음, 무한루프 없음)

---

## 변경 이력

### 2026-08-16

#### 🔧 URL 오염 수정
- `_emerLoadCoName()` 내 `history.replaceState`로 `?did=` 파라미터를 URL에 추가하던 코드 제거
- 링크복사 칸도 `?did=` 없이 슬러그 URL(`mbtico.kr/mbti`)만 표시하도록 변경
- **이전**: `mbtico.kr/mbti?did=9XD2K3W1t...` / **이후**: `mbtico.kr/mbti`

#### ⚡ Firestore 읽기 최적화 3종
- 관리자 기사현황 모니터 자동 갱신: **30초 → 2분** (75% 감소)
- `/api/emergency-map` Worker KV 캐시 **60초** 추가 — 동일 dealerId+날짜 요청 중복 Firestore 조회 차단
- 배송 등록 시 사진 업로드 버그 수정 — `_pendingPhotoUpload` 큐에만 쌓이고 실제 Storage 업로드가 안 되던 dead path 수정, 저장 성공 후 백그라운드 업로드 실행

#### 🧹 코드 정리 (61줄 제거)
- `if(false)` 구버전 구독 체크 블록 37줄 제거
- `_SA_EM` 슈퍼어드민 배열 제거 (dead block에서만 사용)
- 미사용 함수 제거: `doLogin()`, `showLoading()`, `hideLoading()`, `_googleGeocodePin()`
- 디버그 toast `'📷 goOCR호출'` 제거
- `_filoToast` → `toast()` 교체 (FILO 앱 함수 오참조 수정)

---

### 2026-08 (이전 세션)

#### 🔁 무한 Firestore 루프 수정
- 원인: `_emerInitLogin()` → `_emerLoadCoName()` → `.then()` → `_emerInitLogin()` 순환 호출
- 수정: `_coNameLoaded` 플래그로 재진입 차단, `.then()` 콜백에서 `_emerInitLogin()` 제거
- **이전**: 페이지 로드 시 706건+ Firestore 요청 / **이후**: 최대 2건

#### 👥 관리자 / 기사 화면 분리
- 로그인 화면에 탭 추가: 기사 로그인 / 관리자 로그인
- 관리자: 이메일+비밀번호 Firebase Auth 로그인 → 관제 패널 진입
- 기사: 구역 선택 + 이름 입력 → 배송 화면

#### 🗂 관리자 패널 3탭
- **기사 현황**: 오늘 배송 기사별 카드 (2분 자동 갱신)
- **단가 설정**: 구역별 단가 + 서비스(앱) 목록 + 슬러그 등록
- **정산**: 기사별 → 앱명칭별 → 구역별 집계, 소계+총합계

#### 📱 다중 앱 URL 구조
- Worker에 2단계 슬러그 라우팅 추가: `/{slug}/{appSlug}`
- 접속 시 `_pwaAppName` 전역변수 주입 → 기사 화면 앱 고정
- PWA manifest도 앱별 URL 반환: `/{slug}/{appSlug}/manifest.json`
- 고정 앱 URL: `mbtico.kr/mbti/롯데` → 롯데 고정, 다른 앱 선택 불가
- 단기 앱 URL: `mbtico.kr/mbti` → 로그인 시 앱 선택 가능

#### 🔖 기사 자동 등록
- 기사가 앱 URL(`/mbti/롯데`)로 첫 로그인 시 `/api/emergency-register` 호출 → Firestore `companies/{did}.emergencyDrivers` 맵에 자동 등록

#### ✅ 관리자 기사 앱 배정 매트릭스
- 관리자가 기사 현황 탭에서 체크박스로 기사별 앱 배정
- 같은 기사를 여러 앱에 중복 배정 가능 (재등록 불필요)
- `/api/emergency-driver-apps` POST로 저장

#### 🏷 서브타이틀 형식
- **이전**: `회사명 기사 전용`
- **이후**: `회사명 / 앱명칭` (앱 고정 시) 또는 `회사명` (관리자 탭)

#### 📊 정산 앱명칭별 분리
- 배송 저장 시 `appName` 필드 포함
- 정산 탭: 기사 → 앱명칭 → 구역 순 그룹화, 앱별 소계 + 기사별 총합계

---

## Firestore 읽기 현황

| 이벤트 | 읽기 횟수 | 비고 |
|--------|----------|------|
| 앱 최초 로드 | 1회 | `companies/{did}` |
| 기사 로그인 | 0~1회 | localStorage에 did 없을 때만 `users/{uid}` |
| 배송 저장 (운송장 있을 때) | 1회 | 중복 핀 체크 |
| 배송 저장 (운송장 없음) | 0회 | 직접 저장 |
| 관리자 기사현황 | Worker 경유 | 2분마다 `/api/emergency-map` (KV 캐시 60초) |
| 정산/지도 | Worker 경유 | 탭 진입 시 1회 (KV 캐시 60초) |

**onSnapshot 없음** — FILO처럼 탭 열어두어도 연속 읽기 발생 안 함

---

## 알려진 제한사항

- 관리자가 `mbtico.kr/mbti?did=` URL로 직접 접근 시에도 동작 (슬러그 우선, 폴백 지원)
- PWA 홈화면 설치는 앱별 URL에서 설치해야 올바른 start_url 적용
- 배송 사진은 Firebase Storage 사용 (Firestore 읽기 쿼터와 무관, Storage 1GB/일 무료)
